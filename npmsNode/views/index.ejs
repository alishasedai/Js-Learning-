<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EJS page</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <!-- <h2 class="bg-blue-500 text-white">Hello Alisha <%= 2+2 %></h2> -->
    <div class="main w-full min-h-screen ">
        <div class="form p-4 text-white">
            <form action="/create" method="post">
                <input type="text" name="title" class="block w-full rounded-md px-3 py-5 bg-zinc-900" placeholder="Title Goes Here:">
                <textarea name="details" class="block px-3 py-5 bg-zinc-900 w-full h-100 mt-2" placeholder="Write task details here: " ></textarea>
                <input type="submit" class="bg-blue-600 px-5 rounded py-2 mt-2 " value="create task">
            </form>
        </div>
        <div class="tasks flex gap-3 flex-wrap p-4">
            <% if(files.length > 0) {%>
                <% files.forEach(val => { %>
                <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
                <h2 class="text-white text-2xl tracking-tighter">Some Task goes here</h2>
                <a href="" class="text-blue-400 mt-5 inline-block">Read More.</a>
                
            </div>
           <% }); %>
                <% } else {%>
                    <div class="p-3 rounded bg-zinc-500 notask text-white">No tasks yet</div>
            <% }%>
        
           
        </div>
    </div>
</body>
</html>